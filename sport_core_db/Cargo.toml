[package]
name = "sport_core_db"
version = "0.0.1"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_derive = "1.0"
chrono = {version = "0.4", features = ["serde"]}
